$base: top.wmsnp.caps
$version: 1.21.5

caps:
  /requires: LEVEL_GAMEMASTERS
  set:
    max:
      $value int[0, .Caps.MAX_MAX_VEIN_BLOCKS]:
        /executes: .server.ModCommands#setMaxCommand